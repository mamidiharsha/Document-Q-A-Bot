# =============================================================================
# RAG Document Q&A Bot — Dependencies
# =============================================================================

# Google Gemini SDK (LLM + Embeddings)
google-genai>=1.0.0

# Vector Database
chromadb>=0.6.0

# Document Parsing
PyMuPDF>=1.25.0          # PDF extraction (import as fitz)
python-docx>=1.1.0       # DOCX extraction

# CLI Interface
rich>=13.9.0             # Beautiful terminal output
typer>=0.15.0            # CLI framework

# Web UI (Bonus)
streamlit>=1.41.0        # Web interface

# Environment & Config
python-dotenv>=1.0.0     # .env file loading
